[build]
builder = "nixpacks"
buildCommand = "mvn clean package -DskipTests"

[deploy]
startCommand = "java -jar backend/target/backend.jar"
healthcheckPath = "/api/health"
restartPolicyType = "on_failure"
